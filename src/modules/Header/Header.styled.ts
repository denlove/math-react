import styled from 'styled-components'

const StyledHeader = styled.header`
    width: 100%;
    background-color: grey;
    display: flex;
    align-items: center;
    gap: 1rem;
`

export { StyledHeader }
